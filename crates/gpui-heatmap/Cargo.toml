[package]
name = "gpui-heatmap"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui.workspace = true
gpui-component.workspace = true
time = "0.3.54"

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
