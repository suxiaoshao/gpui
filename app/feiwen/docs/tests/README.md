# feiwen 测试步骤文档

本文档与 `../features/` 一一对应。测试目标是验证用户可见行为，不验证 Rust 模块结构。每条测试必须写清具体输入、按钮、观察点和清理方式，方便人工执行，也方便 agent 使用 `computer-use` 执行。

## 数据隔离与预置规则

- 不使用用户真实数据库、真实 Cookie、真实抓取入口或真实导出目录作为测试对象。
- 每次手测使用独立测试数据目录或测试数据库。推荐路径示例：`/tmp/feiwen-qa-data`。
- 如果测试目标不是“抓取真实分页流程”，但需要小说、作者、标签或章节数据作为前置条件，优先在启动 app 前用命令行写入测试 SQLite 数据库。
- 抓取类测试优先使用本地 mock HTTP 服务或测试 fixture。只有专门的 live smoke 测试才允许访问真实站点，并且不能作为质量门禁。
- Cookie 使用测试占位值，不截图、不提交、不记录真实 Cookie。
- 测试结束后删除测试数据库、mock 服务数据、日志截图和临时配置。

## 命令行预置数据建议

具体命令应以当前 Diesel migration 和 schema 为准。建议流程：

1. 创建临时数据目录，例如 `/tmp/feiwen-qa-data`。
2. 使用测试数据目录启动 app 一次，让迁移创建 SQLite 数据库。
3. 关闭 app。
4. 使用 `sqlite3 <测试数据库路径>` 写入测试小说、作者、标签和关联关系。
5. 再次启动 app，确认页面只显示 `QA` 前缀数据。

如果不能确认数据库路径，停止测试并先定位测试环境，不要继续操作默认用户数据目录。

## 标准测试数据

| 类型 | 字段 | 固定值 |
| --- | --- | --- |
| 小说 | 标题 | `QA 星河测试小说` |
| 小说 | 简介 | `这是用于 feiwen QA 的预置简介。` |
| 小说 | 作者 | `QA 作者甲` |
| 小说 | 字数 | `120000` |
| 小说 | 阅读数 | `3456` |
| 小说 | 回复数 | `78` |
| 小说 | 是否受限 | `否` |
| 小说 | 最新章节 | `QA 最新章节标题` |
| 标签 | 标签一 | `QA 标签玄幻` |
| 标签 | 标签二 | `QA 标签完结` |
| 受限小说 | 标题 | `QA 受限测试小说` |
| 受限小说 | 作者 | `QA 作者乙` |
| 抓取 URL | 测试入口 | `http://127.0.0.1:18081/books` |
| 抓取 Cookie | 占位值 | `qa_session=redacted-test-cookie` |
| 页码 | 起始页 | `1` |
| 页码 | 结束页 | `2` |

## 用例格式要求

每个非 README 测试文档必须包含以下结构：

- `测试目标`
- `数据隔离`
- `测试前提`
- `测试数据`
- `测试步骤`
- `预期结果`
- `边缘情况`
- `清理`

步骤必须能直接执行。不要写“填写信息”或“执行查询”；要写“在 `入口链接` 输入框填写 `http://127.0.0.1:18081/books`，点击 `开始抓取` 按钮”。

## 测试目录

- [应用外壳](app-shell/README.md)
- [数据抓取](fetch/README.md)
- [高级检索](query/README.md)
