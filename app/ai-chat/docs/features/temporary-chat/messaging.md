# 临时对话 - 消息交互

## 功能目标

临时对话消息交互提供与普通对话相似的发送、暂停、预览和重发能力，但不依赖会话树。

## 入口

- 打开临时对话窗口后使用底部聊天表单。

## 主要状态

- 空状态。
- 模板选择状态。
- 生成中状态。
- 正常、暂停或错误消息状态。

## 用户动作

- 输入问题。
- 选择模板。
- 发送消息。
- 暂停生成。
- 预览、编辑、删除或重发临时消息。

## 边界情况

- 没有配置模型时显示配置提示。
- 临时消息操作不应影响普通会话树。
- 生成失败应留在临时窗口中可恢复。
