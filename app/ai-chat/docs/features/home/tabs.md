# 主页 - 标签页

## 功能目标

标签页用于在多个会话之间切换，保留用户当前打开的工作上下文。

## 入口

- 从侧边栏选择会话。
- 从搜索结果打开会话。
- 新建或复制会话后打开。

## 主要状态

- 无标签页：显示主页空状态。
- 单标签页：显示当前会话。
- 多标签页：可切换、关闭和保持活动标签。

## 用户动作

- 打开会话。
- 切换标签页。
- 关闭标签页。
- 重启后恢复可恢复的标签页状态。

## 边界情况

- 当前会话被删除后，标签页应关闭或切换到有效状态。
- 会话标题变化后，标签页标题应刷新。
- 标签页恢复不能打开不存在的会话。
